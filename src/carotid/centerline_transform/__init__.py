# image + model --> set of coordinates per slice (original space)
# TODO next version: remove centers of the external carotid which are too close from the internal ones on the same slide
# TODO write only one parameters json, and check if pipeline was already run (if so prevent running or force)
